#pragma once
enum class Controls {
    KB = 0,
    MOUSE = 1,
    AI = 2,
    COUNT = 3
};

enum class items {
	P1, P2, BALL, SCORE1, SCORE2, POWERITEM
};

enum class Menus {
    mainMenu = 0,
    optionsMenu = 1,
    quit = 2,
    none = 3
};